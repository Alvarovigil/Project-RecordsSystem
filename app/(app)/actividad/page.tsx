import ActivityView from "@/components/ActivityView";

export const metadata = {
  title: "Actividad",
  description: "Lo que se mueve alrededor de tu colección: quién añade, quién publica y quién guarda.",
};

export default function ActivityPage() {
  return <ActivityView />;
}
